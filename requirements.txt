# 🍀 Clover v2 dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9
imap-tools>=1.5
keyring>=24
pytest>=8.0
# Phase 1 add-on — shared-link harvesting & download
httpx>=0.27
playwright>=1.40
# Phase 3 — attachment text extraction (PDF text/tables/markup-annotations, spreadsheets, docs)
pymupdf>=1.24
openpyxl>=3.1
python-docx>=1.1
# (OCR for scanned/image attachments is deferred; pytesseract + a Tesseract binary would enable it)
